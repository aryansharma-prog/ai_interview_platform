# API Base URL (Leave empty in local dev to use Vite proxy, or set to Render backend in production)
# Development (default uses Vite proxy /api):
VITE_API_URL=

# Production (e.g. Render backend URL):
# VITE_API_URL=https://your-backend.onrender.com
